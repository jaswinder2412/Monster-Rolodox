# Monster RoloDox Basic Class Based Components

This project is built in react with class based Components

## Available Scripts

In the project directory, you can run:


## `npm install`

To install node_modules packages

### `npm start`

Runs the app in the development mode.\
Open [http://localhost:3000](http://localhost:3000) to view it in your browser.

The page will reload when you make changes.\
You may also see any lint errors in the console.



>>This web project contains basic fetaures like Search, and fetch from api : (https://jsonplaceholder.typicode.com/users)
